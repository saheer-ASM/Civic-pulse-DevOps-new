# AWS Configuration
AWS_REGION=us-east-1
AAWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_ACCOUNT_ID=709741857852

# EKS Configuration
EKS_CLUSTER_NAME=civic-pulse-eks
KUBERNETES_VERSION=1.27

# Docker Hub
DOCKER_USERNAME=moshaheer
DOCKER_PASSWORD=your_docker_hub_password_here
DOCKER_REGISTRY=docker.io

# Application Configuration
NODE_ENV=production
PORT=5000
CLIENT_PORT=3000

# Database Configuration (LOCAL MongoDB for development)
DB_HOST=mongodb
DB_PORT=27017
DB_NAME=civic-pulse
DB_PASSWORD=changeme
MONGODB_URI=mongodb://admin:changeme@mongodb:27017/civic-pulse?authSource=admin

# Backend API
API_URL=http://api.civic-pulse.local/api
API_TIMEOUT=30000
API_RETRY_ATTEMPTS=3

# Frontend Configuration
REACT_APP_API_URL=http://api.civic-pulse.local/api
REACT_APP_ENV=production

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here-change-in-production
JWT_EXPIRATION=7d

# CORS Configuration
CORS_ORIGIN=http://civic-pulse.local

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
DEBUG=false

# Kubernetes Ingress
INGRESS_HOST=civic-pulse.local
INGRESS_CLASS=nginx

# Helm Release
HELM_RELEASE_NAME=civic-pulse
HELM_NAMESPACE=default

# Jenkins Configuration
JENKINS_URL=http://jenkins:8080
JENKINS_API_TOKEN=your_jenkins_api_token_here

# Security
ENABLE_HTTPS=true
CERTIFICATE_ISSUER=letsencrypt-prod

# Monitoring (Optional)
PROMETHEUS_ENABLED=false
GRAFANA_ENABLED=false

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *

# Email Configuration (if needed)
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_email_password
